---
- name: CONTROL NODE 01
  hosts: localhost
  become: yes

  tasks:

    # =========================
    # SYSTEM BASE
    # =========================

    - name: Update system
      apt:
        update_cache: yes
        upgrade: dist

    - name: Install base packages
      apt:
        name:
          - curl
          - git
          - vim
          - htop
          - openssh-server
          - python3-pip
        state: present

    - name: Enable SSH
      systemd:
        name: ssh
        enabled: yes
        state: started


    # =========================
    # DOCKER
    # =========================

    - name: Install Docker
      shell: curl -fsSL https://get.docker.com | sh
      args:
        creates: /usr/bin/docker

    - name: Start Docker
      systemd:
        name: docker
        enabled: yes
        state: started


    # =========================
    # K3S
    # =========================

    - name: Install K3s
      shell: curl -sfL https://get.k3s.io | sh -
      args:
        creates: /usr/local/bin/k3s


    # =========================
    # PYTHON LIBRARIES (FIXED FOR UBUNTU 24.04 / PEP 668)
    # =========================

    - name: Install Python libraries (system-safe)
      apt:
        name:
          - python3-kubernetes
          - python3-requests
        state: present
